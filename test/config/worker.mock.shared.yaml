# 测试用 Worker 配置模板（executor=mock + queue_mode=shared）
# 由 test/test_work.mjs 复制到 <hgfs-root>/config/worker.yaml 使用。
# mock 执行器：不真实执行命令，用于验证队列流转 / 策略校验 / 结果回写链路。
queue_mode: shared
executor: mock
exec_mode: command